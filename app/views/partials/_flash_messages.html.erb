<% flash.each do |k,v| %>
  <div class="alert alert-<%=k%>">
    <button type="button" class="close" data-dismiss="alert" id="close-button">
      &times;
    </button>
    <div class="message">
      <%= display_message(v) %>
    </div>
  </div>
<% end %>
