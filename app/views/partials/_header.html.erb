<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to "The Structure Co", root_path, class: "navbar-brand" %>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#header-dropdown" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <div class="collapse navbar-collapse" id="header-dropdown">
      <ul class="nav navbar-nav navbar-right">
        <li><%= form_tag items_path, class: "form-inline", method: :get do %>
            <%= text_field_tag :query, params[:query], class: "form-control" %>
            <%= submit_tag "Search", class: "btn btn-primary" %>
            <% if params[:query].present? %>
            <% link_to "clear", items_path %>
            <% end %>
            <% end %>
        </li>
        <li><a href="/about">About Us<span class="sr-only">(current)</span></a></li>
        <li><a href="/items">Shop<span class="sr-only"> (current)</span></a></li>
        <li><a href="/faq">FAQ<span class="sr-only">(current)</span></a></li>
        <% if current_user %>
          <li><%= link_to current_user.email, edit_user_registration_path, class: 'white shadowed link-styling' %></li>
          <li><%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'white shadowed link-styling' %></li>
          <li><div class="btn btn-success" data-toggle="modal" data-target="#cartmodal">CART button here</div></li>
        <% else %>
          <li><div class="btn btn-success" data-toggle="modal" data-target="#registrationmodal">REGISTER</div></li>
          <li><div class="btn btn-success" data-toggle="modal" data-target="#loginmodal">LOGIN</div></li>
          <li><%=link_to "Cart", cart_show_path %></li><% end %>
      </ul>
    </div>
  </div>
</nav>
